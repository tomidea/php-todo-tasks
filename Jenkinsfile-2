pipeline{

	agent any

	environment {
		DOCKERHUB_CREDENTIALS=credentials('tomidea')
	}

	stages {

		stage('Build') {

			steps {
				sh 'docker build -t todo-app:latest .'
			}
		}

		stage('Login') {

			steps {
				sh 'echo $DOCKERHUB_CREDENTIALS_PSW | docker login -u $DOCKERHUB_CREDENTIALS_USR --password-stdin'
			}
		}

		stage('Push') {

			steps {
				sh 'docker push todo-app:latest'
			}
		}
	}

	post {
		always {
			sh 'docker logout'
		}
	}

}